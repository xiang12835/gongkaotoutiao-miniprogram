<!--pages/info/detail.wxml-->
<view class="container">
    <view class="art-header">
        <image src="{{article.image}}" class="art-img"/>
        <view class="art-title">{{article.title}}</view>
    </view>
    <view class="art-content">
      {{article.body}}
    </view> 
</view>